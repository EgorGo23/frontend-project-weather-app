import React, { } from 'react';

const CitiesList = (listCitites) => {
   

    return (
        <div className="cities-list">
            <ul>

            </ul>
        </div>
    );
};

export default CitiesList;
